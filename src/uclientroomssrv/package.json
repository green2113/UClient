{
  "name": "uclient-rooms-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "keygen": "node scripts/generate-signing-key.mjs",
    "test": "vitest run",
    "types": "wrangler types src/worker-configuration.d.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.2",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  }
}
